import React from 'react';

const style = {
  width: '100%',
  height: '100vh',
  textAlign: 'center',
};

function Loading() {
  return (
    <div style={style}>
      <h2>Loading...</h2>
    </div>
  );
}

export default Loading;
